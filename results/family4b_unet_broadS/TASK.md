# Family 4b: state-space broadening (BROAD-S) with the U-Net second architecture on P1 — write the runner + 1-seed smoke, do NOT launch the full run

Why: family 4 showed mean-broadening repairs the U-Net 2.8× (10.5 → 3.74, ratio 0.355, P3 ≤ 0.25 not held). State-space broadening (family 1c/1e BROAD-S: training ICs sampled so the training-state cloud covers the switch states in the PCA-99 basis, fixed n=640) cleared 0.25 on all four FNO pairs. This tests it on the second architecture. Rules: predeclare; reuse family 4's U-Net runner (pde_restart/family4_second_architecture/run_family4.py, PREDECLARED.md) and family 1e's BROAD-S sampler (pde_restart/family1e_state_broadening_new_pairs/run_family1e.py, PREDECLARED.md) by import; never modify those directories, recovery/, or pkg_scratch/. Env pde_restart/env/bin/python; source pde_volume/ENV.sh; private JAX_COMPILATION_CACHE_DIR=pde_volume/family4b/jax_cache; threads pinned. The machine is memory-constrained: the runner must run at most 2 concurrent fits.

Predeclare: P1 diffusion/Fisher-KPP r=1.0; U-Net as in family 4 (467,728 params); narrow condition reused from family 4 (do not refit; read its per-seed R); BROAD-S corpus constructed exactly as family 1e for P1 (same sampler, coverage target, n=640, MASTER_SEED derivation); 10 seeds; gates copied from family 4 (G1, G2 ≥ 100, G4, P6, ARCH-PARITY within 3× of FNO e_on, P3 median R_broadS / median R_narrow ≤ 0.25). Deliver run_family4b.py, launch_driver.sh (2 concurrent fits, nice 10), a 1-seed 200-step smoke run (background, ≤10 min) proving the pipeline, LAUNCH_README.md with the exact manager command and a wall-time estimate. Write only inside this directory and pde_volume/family4b.
